# 🔧 Исправление проблемы 404 на Netlify

## Проблема
При прямом переходе на URL `https://atg-edu.netlify.app/station/1` возникает ошибка 404, но при переходе через кнопки все работает.

## Причина
Это классическая проблема Single Page Application (SPA). Когда пользователь переходит напрямую по URL `/station/1`, сервер Netlify пытается найти физический файл по этому пути, но его нет. Vue Router работает только на клиентской стороне.

## Решение

### 1. Создан файл `public/_redirects`
```
/*    /index.html   200
```
Этот файл говорит Netlify: "Для любого URL (`/*`) верни файл `/index.html` со статусом 200".

### 2. Создан файл `netlify.toml`
```toml
[build]
  publish = "dist"
  command = "npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
```

### 3. Проект пересобран
- Выполнена команда `npm run build`
- Файл `_redirects` скопирован в папку `dist`

## Что нужно сделать

### Вариант 1: Автоматический деплой (если настроен)
Если у вас настроен автоматический деплой из GitHub:
1. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Fix 404 error for SPA routes"
   git push
   ```
2. Netlify автоматически пересоберет и задеплоит проект

### Вариант 2: Ручной деплой
1. Зайдите в панель управления Netlify
2. Перейдите в раздел "Deploys"
3. Нажмите "Trigger deploy" → "Deploy site"
4. Или загрузите папку `dist` через "Deploys" → "Deploy manually"

### Вариант 3: Через Netlify CLI
```bash
npm install -g netlify-cli
netlify deploy --prod --dir=dist
```

## Проверка
После деплоя проверьте:
1. ✅ Прямой переход: `https://atg-edu.netlify.app/station/1`
2. ✅ Обновление страницы на `/station/1`
3. ✅ Все остальные маршруты работают

## Файлы, которые были созданы/изменены:
- ✅ `public/_redirects` - правила редиректа для Netlify
- ✅ `netlify.toml` - конфигурация Netlify
- ✅ `dist/_redirects` - скопирован в сборку

## Статус
🟡 **Готово к деплою** - нужно задеплоить изменения на Netlify

---

**Дата:** 23 октября 2025  
**Статус:** ✅ Исправлено локально, ожидает деплоя
