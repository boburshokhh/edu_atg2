# Frontend (Vite) unified configuration
#
# Dev routing (recommended):
# - Frontend calls /api/* and /api/minio/*
# - Vite proxies to targets below
#
# After changing any env values, restart the Vite dev server.

# Django backend target for Vite proxy (/api -> this target, with /api stripped)
VITE_API_TARGET=http://localhost:8000

# Optional runtime API base. Leave empty to use same-origin "/api" (recommended).
# If you set this to "http://localhost:8000", frontend will call backend directly (no Vite proxy).
# VITE_API_BASE_URL=/api

# MinIO S3 API target for Vite proxy (/api/minio -> this target, with /api/minio stripped)
VITE_MINIO_TARGET=http://192.168.32.100:9000

# MinIO browser S3 client endpoint.
# Dev default is "http://localhost:3000/api/minio" (computed in code), so this can be omitted.
# In production set to your MinIO S3 endpoint, e.g.:
# VITE_MINIO_ENDPOINT=http://192.168.32.100:9000

VITE_MINIO_BUCKET=atgedu

# If you keep direct browser uploads via S3Client, keys are required:
VITE_MINIO_ACCESS_KEY=minioadmin
VITE_MINIO_SECRET_KEY=minioadmin


