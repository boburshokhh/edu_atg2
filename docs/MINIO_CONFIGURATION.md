# Конфигурация MinIO для проекта

## ✅ Текущая конфигурация

Проект настроен на работу с MinIO по адресу: **`http://dmed.gubkin.uz:9000`**

### Параметры по умолчанию:
- **Endpoint**: `http://dmed.gubkin.uz:9000`
- **Access Key**: `admin`
- **Secret Key**: `1234bobur$`
- **Bucket**: `atgedu`
- **Протокол**: HTTP (не HTTPS)

## 🔧 Настройка через переменные окружения

Проект поддерживает настройку через переменные окружения для гибкости между продакшеном и локальной разработкой.

### Создание файла `.env`

Создайте файл `.env` в корне проекта (на основе `.env.example`):

```env
# MinIO Configuration
VITE_MINIO_ENDPOINT=http://dmed.gubkin.uz:9000
VITE_MINIO_ACCESS_KEY=admin
VITE_MINIO_SECRET_KEY=1234bobur$
VITE_MINIO_BUCKET=atgedu
```

### Для локальной разработки

Если MinIO запущен локально:

```env
VITE_MINIO_ENDPOINT=http://localhost:9000
VITE_MINIO_ACCESS_KEY=admin
VITE_MINIO_SECRET_KEY=1234bobur$
VITE_MINIO_BUCKET=atgedu
```

### Для продакшена

Используйте продакшен значения:

```env
VITE_MINIO_ENDPOINT=http://dmed.gubkin.uz:9000
VITE_MINIO_ACCESS_KEY=admin
VITE_MINIO_SECRET_KEY=1234bobur$
VITE_MINIO_BUCKET=atgedu
```

## 📝 Как это работает

### Автоматическое определение протокола

Код автоматически определяет, использовать ли HTTPS или HTTP, на основе endpoint:

```javascript
// Если endpoint начинается с https:// → tls: true
// Если endpoint начинается с http:// → tls: false
const isHttps = MINIO_ENDPOINT.startsWith('https://')
```

### Fallback значения

Если переменные окружения не заданы, используются значения по умолчанию:
- Endpoint: `http://dmed.gubkin.uz:9000`
- Access Key: `admin`
- Secret Key: `1234bobur$`
- Bucket: `atgedu`

## 🚀 Использование

### В коде

Конфигурация автоматически загружается из переменных окружения:

```javascript
// src/services/minioService.js
const MINIO_ENDPOINT = import.meta.env.VITE_MINIO_ENDPOINT || 'http://dmed.gubkin.uz:9000'
const MINIO_ACCESS_KEY = import.meta.env.VITE_MINIO_ACCESS_KEY || 'admin'
const MINIO_SECRET_KEY = import.meta.env.VITE_MINIO_SECRET_KEY || '1234bobur$'
const MINIO_BUCKET = import.meta.env.VITE_MINIO_BUCKET || 'atgedu'
```

### Тестирование

Для тестирования используйте:

```bash
# С переменными окружения
VITE_MINIO_ENDPOINT=http://dmed.gubkin.uz:9000 npm run test:minio

# Или без (используются значения по умолчанию)
npm run test:minio
```

## 🔒 Безопасность

### ⚠️ Важно для продакшена

1. **Не коммитьте `.env` файл** в Git (он уже в `.gitignore`)
2. **Используйте безопасные credentials** в продакшене
3. **Ограничьте доступ** к MinIO через файрвол
4. **Используйте HTTPS** если возможно (но сейчас используется HTTP)

### Рекомендации

- Храните production credentials в безопасном месте
- Используйте разные credentials для dev/staging/production
- Регулярно ротируйте секретные ключи

## 🧪 Проверка конфигурации

### 1. Проверка подключения

```bash
npm run test:minio
```

Должно показать:
- ✅ Подключение к MinIO успешно
- ✅ Endpoint: `http://dmed.gubkin.uz:9000`
- ✅ Bucket: `atgedu`

### 2. Проверка переменных окружения

В браузере (консоль разработчика):

```javascript
console.log(import.meta.env.VITE_MINIO_ENDPOINT)
// Должно показать: http://dmed.gubkin.uz:9000
```

## 📊 Структура конфигурации

```
src/services/minioService.js
├── MINIO_ENDPOINT (из env или default)
├── MINIO_ACCESS_KEY (из env или default)
├── MINIO_SECRET_KEY (из env или default)
├── MINIO_BUCKET (из env или default)
└── MINIO_CONFIG
    ├── endpoint
    ├── credentials
    │   ├── accessKeyId
    │   └── secretAccessKey
    ├── forcePathStyle: true
    └── tls (автоматически определяется)
```

## 🔄 Миграция с предыдущей конфигурации

Если вы использовали старую конфигурацию:

1. **Создайте `.env` файл** с нужными значениями
2. **Перезапустите dev сервер**: `npm run dev`
3. **Проверьте работу**: `npm run test:minio`

Старые значения по умолчанию заменены на `http://dmed.gubkin.uz:9000`.

## 🐛 Troubleshooting

### Ошибка подключения

**Проблема**: Не удается подключиться к MinIO

**Решение**:
1. Проверьте доступность сервера: `curl http://dmed.gubkin.uz:9000`
2. Проверьте credentials в `.env`
3. Проверьте, что bucket `atgedu` существует

### Переменные окружения не работают

**Проблема**: Переменные из `.env` не загружаются

**Решение**:
1. Убедитесь, что файл называется `.env` (не `.env.local` или другой)
2. Перезапустите dev сервер после изменения `.env`
3. Переменные должны начинаться с `VITE_`

### CORS ошибки

**Проблема**: CORS ошибки при запросах к MinIO

**Решение**:
1. Настройте CORS в MinIO Console
2. Добавьте ваш домен в разрешенные источники
3. Для локальной разработки добавьте `http://localhost:3000`

