# Настройка Vite Proxy для MinIO

## ✅ Реализовано

Настроено автоматическое проксирование запросов к MinIO через Vite dev server для избежания CORS проблем.

## 🔧 Как это работает

### 1. Vite Proxy Configuration

В `vite.config.js` настроен proxy:

```javascript
proxy: {
  '/api/minio': {
    target: 'http://dmed.gubkin.uz:9000',
    changeOrigin: true,
    rewrite: (path) => path.replace(/^\/api\/minio/, ''),
  }
}
```

### 2. Автоматическая замена URL

В `src/services/minioService.js` добавлена функция `getFrontendUrl()`, которая:

- **В dev режиме**: Заменяет `http://dmed.gubkin.uz:9000/atgedu/...` на `/api/minio/atgedu/...`
- **В production**: Оставляет URL как есть (прямой доступ к MinIO)

### Примеры замены:

```
http://dmed.gubkin.uz:9000/atgedu/video.mp4
  ↓ (в dev режиме)
/api/minio/atgedu/video.mp4
```

## 📝 Использование

### В dev режиме (npm run dev)

Все URL автоматически заменяются на относительные пути через прокси:

```javascript
// В коде:
const url = await getPresignedDownloadUrl('video.mp4')
// url будет: /api/minio/atgedu/video.mp4?X-Amz-...

// Vite proxy автоматически перенаправит на:
// http://dmed.gubkin.uz:9000/atgedu/video.mp4?X-Amz-...
```

### В production (npm run build)

URL остаются прямыми к MinIO:

```javascript
// В коде:
const url = await getPresignedDownloadUrl('video.mp4')
// url будет: http://dmed.gubkin.uz:9000/atgedu/video.mp4?X-Amz-...
```

## 🚀 Преимущества

1. **Нет CORS проблем** в dev режиме - все запросы идут через один домен
2. **Автоматическая работа** - не нужно менять код
3. **Production-ready** - в production используется прямой доступ

## 🔍 Проверка

### 1. Проверка в dev режиме

```bash
npm run dev
```

Откройте консоль браузера и проверьте URL:

```javascript
// Должны быть относительные пути:
/api/minio/atgedu/video.mp4
```

### 2. Проверка прокси

В Network tab браузера вы должны видеть запросы к `/api/minio/...`, которые успешно выполняются.

## ⚠️ Важные моменты

1. **Presigned URLs**: Presigned URLs от AWS SDK уже содержат полный URL. Функция `getFrontendUrl()` заменяет только базовый endpoint, сохраняя query параметры подписи.

2. **Production**: В production режиме прокси не используется, поэтому нужно настроить CORS в MinIO для вашего production домена.

3. **Перезапуск**: После изменения `vite.config.js` нужно перезапустить dev сервер.

## 🐛 Troubleshooting

### Проблема: Прокси не работает

**Решение**:
1. Убедитесь, что dev сервер перезапущен после изменения `vite.config.js`
2. Проверьте, что запросы идут на `/api/minio/...`
3. Проверьте консоль браузера на ошибки

### Проблема: URL не заменяются

**Решение**:
1. Убедитесь, что вы в dev режиме (`import.meta.env.DEV === true`)
2. Проверьте, что URL содержит `MINIO_ENDPOINT`
3. Проверьте консоль браузера

### Проблема: 404 ошибки через прокси

**Решение**:
1. Проверьте, что MinIO доступен по `http://dmed.gubkin.uz:9000`
2. Проверьте настройки proxy в `vite.config.js`
3. Проверьте логи Vite dev server


