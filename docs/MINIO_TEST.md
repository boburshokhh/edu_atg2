# Тестирование доступа к MinIO

## 📋 Описание

Тестовый скрипт для проверки подключения и доступа к MinIO bucket `atgedu` на сервере `https://minio.dmed.gubkin.uz/`.

## 🚀 Запуск теста

```bash
npm run test:minio
```

Или напрямую:

```bash
node test-minio-access.js
```

## ✅ Что проверяет тест

### 1. Подключение к MinIO
- Проверяет доступность сервера
- Проверяет правильность credentials
- Проверяет существование bucket `atgedu`

### 2. Получение списка файлов
- Получает первые 10 файлов из bucket
- Отображает их размеры и пути

### 3. Проверка доступа к файлу
- Проверяет существование файла
- Получает метаданные (размер, тип, дата изменения)
- Генерирует presigned URL

### 4. Поиск видео файла
- Ищет файл `video_2025-11-09_17-39-52.mp4` в разных папках:
  - В корне bucket
  - `videos/`
  - `uploads/`
  - `courses/`
- Генерирует presigned URL для видео (действителен 7 дней)

### 5. Структура папок
- Получает список всех папок в bucket
- Отображает иерархию

## 📊 Пример вывода

```
============================================================
🧪 ТЕСТИРОВАНИЕ ДОСТУПА К MINIO BUCKET
============================================================

📡 Тест 1: Проверка подключения к MinIO...
✅ Подключение к MinIO успешно!
ℹ️  Endpoint: https://minio.dmed.gubkin.uz
ℹ️  Bucket: atgedu

📁 Тест 2: Получение списка файлов из bucket...
✅ Найдено файлов: 15
ℹ️  Первые файлы:
  1. video_2025-11-09_17-39-52.mp4 (1024.50 KB)
  2. stations/image1.jpg (256.30 KB)
  ...

🎥 Тест 4: Поиск видео файла "video_2025-11-09_17-39-52.mp4"...
✅ Видео найдено по пути: video_2025-11-09_17-39-52.mp4
✅ Presigned URL для видео сгенерирован (действителен 7 дней)

============================================================
📊 ИТОГОВЫЙ ОТЧЕТ
============================================================
Подключение к MinIO: ✅ Успешно
Получение списка файлов: ✅ Успешно
Структура папок: ✅ Успешно
Поиск видео: ✅ Найдено

============================================================
🎉 ВСЕ ТЕСТЫ ПРОЙДЕНЫ УСПЕШНО!
============================================================
```

## ⚙️ Конфигурация

Текущие настройки в `test-minio-access.js`:

```javascript
const MINIO_CONFIG = {
  endpoint: 'https://minio.dmed.gubkin.uz',
  region: 'us-east-1',
  credentials: {
    accessKeyId: 'admin',
    secretAccessKey: '1234bobur$'
  },
  forcePathStyle: true,
  tls: true // HTTPS
}

const DEFAULT_BUCKET = 'atgedu'
```

## 🔧 Устранение проблем

### Ошибка подключения

**Проблема**: `ECONNREFUSED` или `ENOTFOUND`
- Проверьте доступность сервера: `https://minio.dmed.gubkin.uz`
- Проверьте интернет-соединение
- Убедитесь, что сервер не заблокирован файрволом

### Ошибка авторизации

**Проблема**: `InvalidAccessKeyId` или `SignatureDoesNotMatch`
- Проверьте правильность `accessKeyId` и `secretAccessKey`
- Убедитесь, что credentials не изменились

### Bucket не найден

**Проблема**: `NoSuchBucket`
- Убедитесь, что bucket `atgedu` существует
- Проверьте права доступа к bucket

### Видео не найдено

**Проблема**: Видео не найдено в bucket
- Проверьте, что файл загружен в MinIO
- Проверьте правильность имени файла
- Убедитесь, что файл находится в одной из проверяемых папок

## 📝 Примечания

- Тест использует те же credentials, что и основное приложение
- Presigned URLs действительны ограниченное время (1 час для обычных файлов, 7 дней для видео)
- Тест не изменяет данные в bucket, только читает их

