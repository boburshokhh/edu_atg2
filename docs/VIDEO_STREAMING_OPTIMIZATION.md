# Оптимизация стриминга видео

## ✅ Реализованные оптимизации

### 1. Загрузка по частям (HTTP Range Requests)

Браузер автоматически использует HTTP Range Requests при воспроизведении видео:
- ✅ **Не загружает весь файл** сразу
- ✅ **Загружает только метаданные** при открытии (preload="metadata")
- ✅ **Загружает сегменты по мере необходимости** во время воспроизведения
- ✅ **Поддерживает перемотку** без загрузки всего файла

### 2. Оптимизация настроек видео

```html
<video
  preload="metadata"      <!-- Загружает только метаданные -->
  playsinline             <!-- Встроенное воспроизведение -->
  crossorigin="anonymous" <!-- CORS поддержка -->
>
```

### 3. Отслеживание буферизации

**События для контроля загрузки:**
- `loadstart` - начало загрузки
- `progress` - прогресс загрузки
- `waiting` - ожидание данных (буферизация)
- `playing` - воспроизведение началось
- `canplay` - готово к воспроизведению
- `canplaythrough` - все загружено

### 4. Визуальные индикаторы

**Прогресс-бары:**
- Основной прогресс (синий) - текущая позиция
- Буферизованный прогресс (светло-синий) - загруженные данные

**Статусы:**
- "Подключение к серверу..."
- "Загрузка видео..."
- "Буферизация..."
- "Перемотка..."

## 🚀 Как это работает

### Процесс загрузки:

1. **Метаданные** (длительность, размер) - загружаются сразу
2. **Старт воспроизведения** - загружается небольшой сегмент для начала
3. **Параллельная загрузка** - пока играет одна часть, загружается следующая
4. **Перемотка** - загружается только нужный сегмент

### HTTP Range Requests:

```
GET /video.mp4 HTTP/1.1
Range: bytes=0-1048575
```

Браузер автоматически запрашивает нужные байты:
```
Range: bytes=0-524287      // Первый сегмент (512 KB)
Range: bytes=524288-1048575 // Второй сегмент
Range: bytes=1048576-1572863 // Третий сегмент
```

## 📊 Метрики и мониторинг

### Отслеживаемые параметры:

1. **networkState** - статус сети:
   - 0: NETWORK_EMPTY - нет источника
   - 1: NETWORK_IDLE - загрузка метаданных
   - 2: NETWORK_LOADING - загрузка данных
   - 3: NETWORK_NO_SOURCE - нет источника

2. **buffered** - буферизованные сегменты:
   ```javascript
   const buffered = video.buffered
   const bufferedEnd = buffered.end(buffered.length - 1)
   const bufferedPercent = (bufferedEnd / duration) * 100
   ```

3. **readyState** - готовность видео:
   - 0: HAVE_NOTHING
   - 1: HAVE_METADATA
   - 2: HAVE_CURRENT_DATA
   - 3: HAVE_FUTURE_DATA
   - 4: HAVE_ENOUGH_DATA

## 🎯 Оптимизация для MinIO

### Presigned URLs для видео:

```javascript
// Генерируем presigned URL с длительным сроком действия
const videoUrl = await minioService.getPresignedDownloadUrl(
  objectName,
  7 * 24 * 60 * 60 // 7 дней
)
```

### Поддержка Range Requests в MinIO:

MinIO автоматически поддерживает HTTP Range Requests при использовании presigned URLs через AWS S3 SDK.

## 🔧 Дополнительные оптимизации

### 1. Автоматическая очистка буфера

При закрытии плеера очищается буфер для экономии памяти:
```javascript
videoRef.value.removeAttribute('src')
videoRef.value.load()
```

### 2. Умная буферизация

Плеер автоматически определяет, когда нужно буферизовать:
- При буферизации показывает индикатор
- Автоматически возобновляет воспроизведение при достаточном буфере

### 3. Обработка ошибок сети

- `stalled` - загрузка остановилась
- `suspend` - браузер приостановил загрузку (экономия трафика)
- Показываются статусы для пользователя

## 📈 Производительность

### Преимущества:

✅ **Быстрый старт** - начало воспроизведения через несколько секунд  
✅ **Экономия трафика** - загружается только просматриваемая часть  
✅ **Поддержка перемотки** - мгновенный переход к любой части  
✅ **Адаптивность** - автоматическая подстройка под скорость сети  

### Рекомендации:

1. **Размер сегментов**: Браузер автоматически определяет оптимальный размер
2. **Буферизация**: Обычно буферизуется ~30 секунд вперед
3. **Перемотка**: При перемотке загружается новый сегмент с нужной позиции

## 🎬 Пример использования

```vue
<VideoPlayer
  v-model="showVideoPlayer"
  :video-url="videoUrl"
  :video-title="lesson.title"
  @video-end="handleVideoEnd"
/>
```

**При открытии:**
1. Загружаются метаданные (длительность, размер)
2. Начинается загрузка первого сегмента
3. Воспроизведение начинается после загрузки минимального буфера
4. Продолжается загрузка следующих сегментов параллельно

## 🔒 Безопасность

- Используются presigned URLs для безопасного доступа
- CORS настроен для кроссдоменных запросов
- Анонимный доступ только на чтение

## ✅ Итог

Видеоплеер оптимизирован для стриминга:
- ✅ Загрузка по частям (не весь файл сразу)
- ✅ Быстрый старт воспроизведения
- ✅ Эффективная буферизация
- ✅ Поддержка перемотки
- ✅ Визуальные индикаторы прогресса
- ✅ Автоматическая обработка ошибок

**Все работает автоматически!** 🎉

