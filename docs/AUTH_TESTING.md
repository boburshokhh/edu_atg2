# Инструкция по тестированию авторизации

## 🚀 Запуск приложения

```bash
npm run dev
```

Приложение будет доступно по адресу: `http://localhost:5173`

## 🔐 Тестирование авторизации

### Шаг 1: Переход на страницу входа
1. Откройте браузер
2. Перейдите по адресу `http://localhost:5173/login`

### Шаг 2: Тестирование входа
Используйте любые из следующих учетных данных:

#### Администратор
- **Логин:** `admin`
- **Пароль:** `password123`

#### Инструктор
- **Логин:** `instructor1`
- **Пароль:** `password123`

#### Обычный пользователь
- **Логин:** `student1`
- **Пароль:** `password123`

### Шаг 3: Проверка функциональности
После успешного входа проверьте:

1. **Перенаправление** - должны попасть на `/dashboard`
2. **Отображение имени пользователя** в шапке сайта
3. **Меню пользователя** - кликните на аватар в шапке
4. **Выход из системы** - нажмите "Выход" в меню пользователя

## 🐛 Устранение проблем

### Ошибка "Пользователь не найден"
- Проверьте правильность написания логина
- Убедитесь, что пользователь активен в базе данных

### Ошибка "Неверный пароль"
- Используйте пароль: `password123`
- Проверьте раскладку клавиатуры

### Ошибка подключения к Supabase
- Проверьте интернет-соединение
- Убедитесь, что API ключ правильный

## 📊 Проверка в базе данных

Для проверки пользователей в Supabase:

```sql
-- Все пользователи
SELECT username, full_name, role, is_active FROM users ORDER BY role, username;

-- Конкретный пользователь
SELECT * FROM users WHERE username = 'student1';

-- Активные сессии
SELECT u.username, us.created_at, us.last_activity 
FROM user_sessions us
JOIN users u ON us.user_id = u.id
WHERE us.expires_at > NOW();
```

## 🔧 Отладка

### Проверка консоли браузера
1. Откройте Developer Tools (F12)
2. Перейдите на вкладку Console
3. Попробуйте войти в систему
4. Проверьте ошибки в консоли

### Проверка Network запросов
1. Откройте Developer Tools (F12)
2. Перейдите на вкладку Network
3. Попробуйте войти в систему
4. Проверьте запросы к Supabase

## ✅ Ожидаемый результат

При успешной авторизации:
- ✅ Перенаправление на `/dashboard`
- ✅ Отображение имени пользователя в шапке
- ✅ Доступ к защищенным маршрутам
- ✅ Возможность выхода из системы
- ✅ Сохранение сессии при обновлении страницы

## 📝 Логи для отладки

В консоли браузера должны появиться логи:
```
Login error: [если есть ошибки]
Session check error: [если есть ошибки]
```

В случае успеха логи не должны содержать ошибок.

---

**Примечание:** Если возникают проблемы, проверьте что:
1. Приложение запущено (`npm run dev`)
2. Интернет-соединение работает
3. Используете правильные учетные данные
4. RLS отключен для тестирования (в продакшене нужно будет настроить правильно)
