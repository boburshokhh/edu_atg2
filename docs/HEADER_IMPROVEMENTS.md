# Обновление Header.vue - Улучшение видимости текста

## Проблема
Текст в Header был плохо виден на некоторых страницах из-за сложной логики определения стилей. На главной странице использовался белый текст на прозрачном фоне, а на других страницах - темный текст на белом фоне, но логика была слишком сложной и не всегда работала корректно.

## Решение
Упростили логику определения стилей, создав единый computed property `isLightTheme`, который определяет, когда использовать светлую тему (темный текст на белом фоне).

### Изменения в коде:

#### 1. Добавлен computed property для определения темы
```javascript
// Определяем, нужно ли использовать светлую тему (темный текст на белом фоне)
const isLightTheme = computed(() => {
  return isScrolled.value || route.name !== 'Home'
})
```

#### 2. Упрощена логика стилей
Вместо сложных условий:
```javascript
// Старый код (сложный)
:class="(isScrolled || $route.name !== 'Home') ? 'text-gray-700 hover:text-gray-900' : 'text-white/90 hover:text-white'"

// Новый код (простой)
:class="isLightTheme ? 'text-gray-700 hover:text-gray-900' : 'text-white/90 hover:text-white'"
```

#### 3. Обновлены все элементы Header
- Навигационные ссылки
- Переключатель языков
- Кнопки авторизации
- Меню пользователя
- Мобильное меню
- Кнопка мобильного меню

## Логика работы

### Светлая тема (isLightTheme = true)
Используется когда:
- Пользователь прокрутил страницу вниз (`isScrolled = true`)
- ИЛИ пользователь находится НЕ на главной странице (`route.name !== 'Home'`)

**Стили светлой темы:**
- Фон: `bg-white/95 backdrop-blur-md shadow-sm border-b border-gray-100`
- Текст: `text-gray-700 hover:text-gray-900`
- Hover эффекты: `hover:bg-gray-100`

### Темная тема (isLightTheme = false)
Используется только когда:
- Пользователь НЕ прокрутил страницу (`isScrolled = false`)
- И пользователь находится на главной странице (`route.name === 'Home'`)

**Стили темной темы:**
- Фон: `bg-transparent`
- Текст: `text-white/90 hover:text-white`
- Hover эффекты: `hover:bg-white/10`

## Результат

### ✅ Улучшения:
1. **Лучшая читаемость** - текст всегда виден на фоне
2. **Упрощенная логика** - один computed property вместо сложных условий
3. **Консистентность** - одинаковое поведение на всех страницах
4. **Легкость поддержки** - проще понимать и изменять код

### 📱 Поддержка устройств:
- **Desktop** - корректное отображение на всех размерах экрана
- **Mobile** - адаптивное мобильное меню с правильными стилями
- **Tablet** - промежуточные размеры работают корректно

### 🎨 Визуальные улучшения:
- Плавные переходы между темами
- Консистентные hover эффекты
- Правильные цвета для всех состояний
- Адаптивные отступы и размеры

## Тестирование

### Проверьте на следующих страницах:
1. **Главная страница (`/`)** - белый текст на прозрачном фоне
2. **Страница станций (`/stations`)** - темный текст на белом фоне
3. **Страница входа (`/login`)** - темный текст на белом фоне
4. **Дашборд (`/dashboard`)** - темный текст на белом фоне
5. **Профиль (`/profile`)** - темный текст на белом фоне

### Проверьте прокрутку:
- На главной странице при прокрутке вниз фон должен стать белым, а текст - темным
- На других страницах фон всегда белый, текст всегда темный

### Проверьте мобильную версию:
- Мобильное меню должно использовать правильные стили
- Кнопки должны быть видны на всех страницах
- Переключатель языков должен работать корректно

---

**Примечание:** Изменения обратно совместимы и не влияют на функциональность, только улучшают визуальное отображение.

