## Frontend env for Docker Compose (recommended)
## Copy this file to .env next to docker-compose.yml for Docker deploy.
## Restart `npm run dev` after changing (local dev).

# In Docker: frontend nginx proxies /api -> backend, so runtime base is always /api
VITE_API_BASE_URL=/api

# Dev proxy targets (only used by Vite dev server)
# - Local dev (outside Docker): set backend to localhost
VITE_API_TARGET=http://localhost:8000

# MinIO:
# - In Docker: backend presigns URLs with MINIO_ENDPOINT=http://minio:9000
#   Frontend MUST use the same value to rewrite to /api/minio.
# - For local dev against external MinIO, override these in your local .env.
VITE_MINIO_TARGET=http://minio:9000
VITE_MINIO_ENDPOINT=http://minio:9000
VITE_MINIO_BUCKET=atgedu
VITE_MINIO_ACCESS_KEY=admin
VITE_MINIO_SECRET_KEY=1602atgbobur

# LDAP (frontend toggle only)
VITE_LDAP_ENABLED=true
VITE_LDAP_SERVER=ldap://DC03.atg.uz
VITE_LDAP_BASE_DN=dc=company,dc=com
VITE_LDAP_USER_DN=ou=Users,dc=company,dc=com
VITE_LDAP_USER_PASSWORD=
VITE_LDAP_USER_SEARCH_BASE=ou=Users,dc=company,dc=com
VITE_LDAP_USER_SEARCH_FILTER=(sAMAccountName={username})

